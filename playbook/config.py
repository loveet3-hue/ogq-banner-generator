# -*- coding: utf-8 -*-
"""지표 정의 선택지. 숫자의 '뜻'이 바뀌는 곳이라 여기 모아 둔다."""

# 정지형/애니메이션 비중을 무엇으로 잴 것인가.
#   'cnt'   판매 건수(콘텐츠 판매 수) 기준  ← 현재값. 덱 전체가 이 잣대를 쓴다.
#   'rev'   매출액 기준
#   'buyer' 구매자 수 기준
#
# 기준을 바꾸면 캡션 문구도 같이 손봐야 한다. '매출 비중'과 '선호 비율'은 다른 말이고,
# 채팅+는 기준에 따라 우위가 뒤집힌다(매출 애니 58.3% vs 건수 정지형 54.9%).
#
# ※ 주의: v2 덱의 SOOP '애니메이션 90%'와 채팅+ '애니메이션 72%'는 위 세 기준 어느
#   것과도 맞지 않는다(실측: SOOP 건수 61.2 / 매출 70.4 / 구매자 50.8, 채팅+ 건수
#   45.1 / 매출 58.3 / 구매자 45.7). 손으로 넣은 값이거나 다른 출처로 보인다.
#   자동화하면 이 두 값이 크게 바뀌므로 run.py가 실행 때마다 경고로 알려준다.
CTYPE_BASIS = 'cnt'

# 그 밖의 모든 지표(시간대·요일·월별·테마·집중도·키워드·TOP25 가중치)가 쓰는 잣대.
# 유형 비중만 판매 수로 재고, 나머지는 매출액으로 잰다.
BASIS = 'rev'

# 인기 키워드로 인정할 최소 크리에이터 수 (특정 콘텐츠 지목 방지)
KEYWORD_MIN_CREATORS = 5
KEYWORD_COUNT = 8

# 시간대 '집중 구간'을 만들 누적 비중(%)
HOUR_WINDOW_TARGET = 65.0

# 크리에이터 배포용 덱에 절대 나오면 안 되는 것 — qa.py가 검사
FORBID_ABSOLUTE = True
